spring:
  datasource:
    url: jdbc:h2:mem:parkingtest
    driverClassName: org.h2.Driver
    username: root
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update